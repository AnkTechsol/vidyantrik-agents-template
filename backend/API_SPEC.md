# Vidyantrik Backend API Specification

Generated by Claude AI - Backend API skeleton

## Tech Stack
- Node.js + Express.js
- JWT Authentication
- MongoDB (planned)
- CORS enabled

## API Structure

### File Organization
```
backend/
├── server.js              # Main entry point
├── routes/
│   ├── auth.js           # Authentication routes
│   ├── courses.js        # Course management
│   ├── quizzes.js        # Quiz submission
│   └── certificates.js   # Certificate generation
├── middleware/
│   └── auth.js           # JWT auth middleware
└── config/
    └── database.js       # DB connection
```

## API Endpoints

### Authentication
- `POST /api/auth/register` - User registration
- `POST /api/auth/login` - User login

### Courses
- `GET /api/courses` - List all courses
- `GET /api/courses/:id` - Get course details
- `POST /api/courses` - Create course (authenticated)
- `PUT /api/courses/:id` - Update course (authenticated)
- `DELETE /api/courses/:id` - Delete course (authenticated)

### Quizzes
- `POST /api/quizzes` - Submit quiz answer
- `GET /api/quizzes/course/:courseId` - Get course quizzes
- `GET /api/quizzes/submissions/:userId` - User submissions

### Certificates
- `GET /api/certificates/:userId/:courseId` - Get certificate

## Response Format
All API responses follow this structure:
```json
{
  "success": true/false,
  "message": "...",
  "data": {...}
}
```

## Status
API skeleton generated. Full implementation in progress.
